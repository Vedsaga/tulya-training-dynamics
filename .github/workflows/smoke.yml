name: smoke

on:
  push:
  pull_request:

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Compile experiment
        run: python -m py_compile kaggle_grokking_experiment.py experiment2_core.py experiment2_eval.py experiment2_v3.py
